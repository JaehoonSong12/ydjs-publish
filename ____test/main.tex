% (1) .tex Project General Configuration
\documentclass[11pt]{article}
\hbadness=10000 % Suppress Warnings (Horizontal)
\vbadness=10000 % Suppress Warnings (Vertical)



\input{./macros.tex}
% \input{../preamble/symbol.tex}
% \input{../preamble/opeartor.tex}

\input{../preamble/group.tex}


% \input{../preamble.tex}
% \input{../preamble/font.tex}
% \input{../preamble/math.tex}


% (2) .tex Project Specific Configuration
% %%% Sets page margins, overriding 11pt article defaults
% \setlength{\topmargin}{0 in}
% \setlength{\headheight}{0 in}
% \setlength{\headsep}{0 in}
% \setlength{\oddsidemargin}{0in}
% \setlength{\textwidth}{6.5 in}
% \setlength{\textheight}{9 in}

% %%% Useful for layout
% \newcommand{\VF}{\vspace*{\fill}}
% \newcommand{\HF}{\hspace*{\fill}}



% % (3) Configuration Reference
% % choose your reference generation style:
% %   - for humanities: chicago-authordate, apa, mla
% %   - for sciences: numeric, numeric-comp, alphabetic
% %   - 
% \usepackage[
%   backend=biber,           % use biber for processing
%   style=numeric,           % bibliography style
%   citestyle=authoryear,    % in-text citations
%   sorting=ynt,             % year-name_of_auther–title order
%   % maxcitenames=2,          % limit authors in citations
%   % bibencoding=utf8         % if your .bib is UTF-8
% ]{biblatex}
% \addbibresource{citations.bib}











\begin{document}


  The inner product $\coord{x,y}$ defines a metric.
  The group $\group{G}$ acts on the set $\set{X}$.
  The absolute value $\abs{x}$ is always non-negative.
  The interval $\arr{a,b}$ contains all real numbers between a and b.



  % The statement P \AND Q is true.
  % If P \THEN Q, then \NOT Q \THEN \NOT P.
  % P \IFF Q means P \THEN Q \AND Q \THEN P.
  % \NOT{p} \AND{q}{r} \THEN{s}{t}
  % The union A \sOR B of sets A and B.
  % The complement \sNOT{A} of set A.
  % The power set \sPOWER(X) of set X.
  % The cardinality \sCARD(A) of set A.
  % \sOR{A}{B} \sAND{C}{D} \sMINUS{E}{F}
  % The union $\sORALL{i=1}{n}{A_i}$ of sets $A_1$ through $A_n$.
  % The intersection $\sANDALL{j=0}{k}{B_j}$ of sets $B_0$ through $B_k$.
  % The set $\sBUILD{x}{x > 0}$ of positive real numbers.
  % The set $\sBUILD{n}{n \in \mathcal{N} \AND n \text{ is even}}$ of even naturals.

  % Hi,

  % p \AND q          % infix in text/math (ensure math mode around p,q if needed)

  % \AND{p}{q}        % functional form

  % $p \AND q$        % math mode infix

  % $\AND{p}{q}$      % math mode functional

  % \NOT              % symbol only

  % \NOT{p}           % prefix form of negation



  \newpage





  
  % The set \setN contains all natural numbers.
  % For any $x \in \setR$, we have $x^2 \geq 0$.
  % The complex numbers \setC extend the reals.

  % The probability \Prob(A) of event A.
  % The expected value \E[X] of random variable X.
  % The standard deviation \sd of the distribution.

  % X $\sim$ \Bernoulli(p) follows a Bernoulli distribution.
  % Y $\sim$ $\Normal(\mu,\sigma^2)$ is normally distributed.
  % Z $\sim$ $\Exponential(\lambda)$ has exponential distribution.

  % The space \opC of continuous functions.
  % The Hilbert space \opH with inner product.
  % The power set \opP(X) of set X.

  % The structure sheaf \shO of the scheme.
  % The Schwartz space \shS of rapidly decreasing functions.
  % The sheaf \shF of functions on the manifold.

  % The Lie algebra \algg with bracket operation.
  % The special linear algebra \algsl(n).
  % The prime ideal \algp in the ring.


  
  % The set \setN contains all natural numbers.
  % For any $x \in \setR$, we have $x^2 \geq 0$.
  % The complex numbers \setC extend the reals.


  \begin{enumerate}
    \item 
    Prove the following directly from the two definitions and three axioms
    given in ICA2 on 8/20.
    % \begin{define}{ICA2 Definitions and Axioms}
    %   Here are the ICA2 Definitions and Axioms as follows.
    %   \begin{enumerate}
    %     \item \textbf{Definition:} An integer $n$ is \underbar{even} if there exists some integer $k$ such that
    %     \[
    %     n = 2k.
    %     \]
    %     \item \textbf{Definition:} An integer $n$ is \underbar{odd} if there exists some integer $k$ such that
    %     \[
    %     n = 2k + 1.
    %     \]
    %     \item \textbf{Axiom 1:} The sum of two integers is again an integer. (Closure under addition)
    %     \item \textbf{Axiom 2:} The product of two integers is again an integer. (Closure under multiplication)
    %     \item \textbf{Axiom 3:} An integer is odd if and only if it is not even.
    %   \end{enumerate}
    % \end{define}
    You may not use any other results without proving them first.
    Every statement in your proof must be fully justified.
    See \textbf{Elements of (Proof) Style} guidelines in Canvas for information.
    \begin{enumerate}
    \item Direct proof: For all integers $n$, if $n$ is odd, then $9n + 5$ is even.
    % \begin{subprove}
    %   Let $n$ be an arbitrary integer then assume $n$ is odd for the sake of direct proof. 
    %   By the definition of odd, there exists an integer $k$ such that
    %   \[
    %     n = 2k + 1.
    %   \]
    %   By substitution, 
    %   \[
    %     9n + 5 = 9(2k + 1) + 5 = 18k + 9 + 5 = 18k + 14= 2(9k + 7).
    %   \]
    %   By Axiom 1 and 2 (closure under addition and multiplication), $9k+7$ is an integer since $9$, $k$, and $7$ are integers. (Trivial)

    %   Therefore, by the definition of even, $9n+5$ is even.
    % \end{subprove}

    \item Contrapositive proof: For all integers $n$, if $7n + 5$ is odd, then $n$ is even.\\
    % \begin{subanswer}
    %   \textbf{Claim.} For every integer $n$, if $7n+5$ is odd then $n$ is even.\\

    %   \textbf{Remark (justifying the contrapositive method).} The implication $P \THEN Q$ is logically equivalent to its contrapositive $\NOT Q \THEN \NOT P$, because both are false exactly in the case $P$ is true and $Q$ is false. Hence proving the contrapositive suffices.\\

    %   \textbf{Proof (by contrapositive).} The contrapositive of the statement is: \inlinequote{If $n$ is odd, then $7n+5$ is even.} Assume $n$ is odd. By definition of odd there exists integer $k$ with $n = 2k + 1$. Then
    %   \[
    %     7n + 5 = 7(2k+1) + 5 = 14k + 7 + 5 = 14k + 12 = 2(7k + 6).
    %   \]
    %   By Axiom 2 and Axiom 1, $7k+6$ is an integer, so $7n+5$ equals $2$ times an integer. By the definition of even, $7n+5$ is even. Thus the contrapositive is proved, and therefore the original implication holds: if $7n+5$ is odd then $n$ is even.
    % \end{subanswer}

    \item Proof by contradiction: For all integers $n$, if $3n + 2$ is even, then $n$ is even.\\
    % \begin{subanswer}
    %   \textbf{Claim.} For every integer $n$, if $3n+2$ is even then $n$ is even.\\

    %   \textbf{Proof (by contradiction).} Assume, for contradiction, that $3n+2$ is even and $n$ is \emph{not} even. By Axiom 3, $n$ is not even implies $n$ is odd. Thus there exists integer $k$ with $n = 2k + 1$. Compute:
    %   \[
    %     3n + 2 = 3(2k+1) + 2 = 6k + 3 + 2 = 6k + 5.
    %   \]
    %   Write $6k+5 = 2(3k+2) + 1$, so $6k+5$ is of the form $2(\text{integer})+1$, hence odd by the definition of odd. This contradicts the assumption that $3n+2$ is even. Therefore the assumption that $n$ is not even is false, so $n$ must be even. This completes the proof by contradiction.
    % \end{subanswer}
    \end{enumerate}
  \end{enumerate}
  





  %% MATH 2106: Foundations of Mathematical Proof
  % \input{math2106/syllabus.tex}
  % \input{math2106/lecture01.tex}
  % \input{math2106/test.tex}

  % \input{math2106/proof01.tex}
  % \input{math2106/proof02.tex}

  % \input{math2106/hw00.tex}
  % \input{math2106/hw01.tex}




  %% MATH 4150: Number Theory
  % \input{math4150/lecture01.tex}


  %% MATH 2552: Differential Equations
  % \input{math2552/diffeq01.tex}
  % \input{math2552/diffeq02.tex}
  % \input{math2552/diffeq03.tex}



  %% MATH 3012: Combinatorics
  % \input{math3012/comb01.tex}

\end{document}

